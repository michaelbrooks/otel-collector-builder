# Centralized version configuration for OpenTelemetry Collector Builder
# Used by build scripts and CI/CD workflows

# OpenTelemetry Collector Builder version
OTEL_BUILDER_VERSION=v0.139.0

# OpenTelemetry packages version (used in manifest.yaml)
OTEL_PACKAGE_VERSION=v0.139.0

# Build version - defaults to latest git tag if not specified
# Override: BUILD_VERSION=v1.0.0 ./scripts/build_in_docker.sh
BUILD_VERSION=${BUILD_VERSION:-$(git describe --tags --abbrev=0)}
